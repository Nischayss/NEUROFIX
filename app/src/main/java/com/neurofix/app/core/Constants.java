package com.neurofix.app.core;

/**
 * Deliberately minimal. Only add a constant here when a real feature needs
 * it — no speculative entries for unbuilt features (Focus Modes, Scheduling,
 * etc.), per the Version 1 "no speculative work" rule.
 */
public final class Constants {

    private Constants() {
    }

    // Populated starting with the Onboarding step.
}
